with Ada.Text_IO; use Ada.Text_IO;
with recursif1; use recursif1;
with mc91; use mc91;
with recursif3; use recursif3;

procedure Main is
   --un_tab : TAB_ENTIERS;
   --taille : Integer := 100;
   --nb : Integer;
begin
   --for i in 0..110 loop --mc
   --nb := mc(i);
   --Put(Integer'Image(nb));
   --end loop;

   --for i in 1..NMAX loop   --initialiser le tableau
      --un_tab(i) := i;
   --end loop;
   --nb := indice(un_tab, 100, 54);
      --Put_Line(Integer'Image(nb));

   affiche(20, 10);
end Main;
